# Workspaces

Panel ML representa operaciones comerciales mediante workspaces. En esta fase,
los usuarios existentes pertenecen a `SAEL` y cada usuario puede integrar un
único workspace.

El registro público no asigna membresías automáticamente. Todo usuario nuevo
debe incorporarse explícitamente a `workspace_members` antes de utilizar las
integraciones comerciales.
